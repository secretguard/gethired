# Agent State

status: complete

## Backlog
(reconciled against AGENT_MASTER_PROMPT.md, 2026-08-01)

### P0 — all done, merged to main and verified on the live production deployment
1. [x] CV-matching pluralization/punctuation fix — done (commit e3d17b3, verified: 16/16 tests pass, build clean). Covers plural suffixes, ISO/IEC long form, slash-vs-x shift notation, dotted-acronym punctuation variants, with negative tests guarding against false positives on short acronyms.
2. [x] Advisory-framing fixes — done (commit bce560b). "Missing" -> "Worth adding" with explainer line in UI + email; education excluded from actionable recommendations and scoring, broadened degree corpus, reframed as informational.
3. [x] Full visual/UX redesign (Tailwind) — done (commit cc33531, merged to main and pushed). New design system: custom color tokens (fog/paper/ink/slate/beacon/verified), Space Grotesk + IBM Plex Sans/Mono type pairing, a "CH.0x checkpoint" motif carried through result cards and the emailed report, coverage bars, decorative scan-strip on the homepage. Applied to upload page, results view, and email template. Verified with a real Chromium browser at desktop and 390px mobile widths (screenshots showed clean rendering, no layout breakage, no console errors) and the rendered email HTML. Added a permanent Playwright E2E smoke suite (`e2e/cv-screener.spec.ts`) covering the upload-to-results flow at both viewport sizes, replacing the one-off scratch scripts used to verify this — this is now regression coverage for future redesign work, not just a one-time check. Gated the debug email-preview route (`/api/debug-email-preview`) to non-production. Added `.gitattributes` marking `*.pdf`/`*.docx` as binary after noticing Windows `core.autocrlf` was corrupting the checked-in PDF test fixture on checkout — caught and fixed before it could silently break the fixture for anyone else cloning the repo.

### P1 — done, merged to main and verified on the live production deployment
4. [x] Practical assessment (checkpoint/flag-based scenarios) — done (commit 0512513). New `lib/assessment` module: 10 original scenarios / 23 checkpoints (2 scenarios per category × 5 categories: log_analysis, networking, vulnerability_identification, owasp_top10, incident_response), each pairing one static artifact with 2-3 sequential questions, graded server-side by exact-match/synonym comparison (`lib/assessment/matcher.ts`) — no AI, no partial credit. `GET /api/assessment` strips the answer key before sending prompts to the client; `POST /api/assessment` grades and persists to the previously-unused `lab_scores` Supabase table (graceful no-op if Supabase isn't configured, same pattern as screenings). Replaced `PracticalAssessmentPlaceholder` with a real `PracticalAssessment` component in the unified report (web + email) showing actual per-category scores and per-checkpoint correct/missed feedback. Verified end-to-end in a real browser (full form render, mixed-correctness scoring producing the expected score, email preview with the new assessment section) plus 13 new unit tests and 2 new Playwright E2E tests (100%-correct flow, mixed-correctness flow).

### P2 — done, merged to main and verified on the live production deployment
5. [x] Roadmap generator — done (commit e0fc5dd). New `lib/roadmap` module combines `generateRecommendations()`'s output (Phase 5) with the assessment's category gaps (Phase 4) into a sequenced, multi-step plan. Config-driven (`data/roadmap-config.json`): an ordered list of 5 stages (fundamentals → hands-on practice → certifications → scripting → profile polish), each mapping CV + assessment categories to a title/intro. A stage only appears if it has a real gap; CV-side gating reuses the recommendation engine's own thresholds rather than duplicating them, so the two systems can't drift out of sync. Actions combine CV recommendations with the assessment's specific missed-checkpoint explanations, capped at 3 per stage. Removed the `LabScore` type and unused `labScores?` parameter from `lib/recommendations` since the actual integration ended up being a higher-level module (this one) rather than threading assessment data through `generateRecommendations` itself — that placeholder anticipated a different shape than what got built, so it was dead weight once the real thing existed. Rendered via a new `RoadmapView` component once the practical assessment completes (numbered/connected step list — the visual mindmap is explicitly P3, not built here). Verified end-to-end in a real browser (steps sequence correctly by stage priority regardless of input order, mixed-gap and all-CV-gap cases both render correctly, no console errors) plus 6 new unit tests and 2 new Playwright E2E tests.

### P3 (only after P0-P2 solid — they are, as of this session)
6. [x] MCQ-style knowledge checks — done (commit 07e9d47, merged to main, verified live). New `lib/mcq` module: 10 original questions (2 per actionable CV category — certifications, tools, concepts_frameworks, scripting_programming, soft_skills; education excluded same as elsewhere), same architectural shape and server-only-answer-key discipline as `lib/assessment`. Deliberately kept standalone: its own page (`/quiz`, linked from the homepage), not gated behind a CV screening, not persisted to Supabase, not fed into the roadmap — a lighter-weight companion, not folded into the combined CV+assessment system. Verified end-to-end in a real browser (full form, submit-disabled-until-complete guard, mixed-correctness scoring isolating misses to the right category) plus 7 unit tests and 3 Playwright E2E tests.
7. [x] Visual roadmap/mindmap view — done (commit 5580f1a, merged to main, verified live). Researcher subagent compared React Flow (ruled out — 57KB+ gzipped, ships pan/zoom/interactivity that all needs disabling for 5 static read-only nodes, SSR indirection) against lighter tree/org-chart libraries and a hand-rolled-SVG option; recommended `react-organizational-chart` (6.5KB, zero extra deps, Tailwind-native styling via children props). `RoadmapView` now toggles between a Diagram view (default) and the original List view, both rendering the same `generateRoadmap()` output — no parallel data model. Two real implementation issues hit and fixed, not worked around: the library touches `document` at module scope, breaking Next's prerender pass even inside a "use client" component (fixed with `next/dynamic({ ssr: false })`); and an initial design nesting every stage as a child of the previous stage's action leaves made the org-chart's total width balloon additively across all stages (2500px+ wide for a 4-step roadmap, discovered via direct DOM bounding-box inspection, not just eyeballing a screenshot) — fixed by giving each stage its own small independent tree stacked and connected vertically, which reads better as a step-by-step diagram besides fixing the width bug. Verified end-to-end in a real browser at desktop and mobile widths (diagram renders correctly, toggle works both directions, horizontal-scroll fallback confirmed for stages whose action count doesn't fit a narrow viewport, zero console errors) plus updated/new Playwright E2E tests.

### P4 — done, merged to main and verified on the live production site
8. [x] Cross-link with sarathg.me — done (`D:\web` commit 0be8595, merged to `main` and pushed, live at `sarathg.me/gethired.html`). Cloned `D:\web` (github.com/secretguard/web) for the first time. Added a new standalone page (`gethired.html`) introducing GetHired, styled to match the site's existing "services" content-page design system (dark bg, Space Grotesk, green accent) rather than copying the Tailwind homepage or an app-shell lab page verbatim — hero, "three tools" breakdown, audience section, how-it-works steps, a "recommended reading" section cross-linking GetHired's skill-gap categories to this site's existing Wazuh/OSINT/ShopEasy/Pivoting guides, FAQ, and a CTA to `gethired.sarathg.me`. Wired in exactly the way `pivoting.html` was: one new top-level navbar link (desktop + mobile, `navbar.js`), one new Featured Projects card (`index.html`), one new `sitemap.xml` entry — no existing page content or structure touched. Chose a standalone top-level nav link rather than nesting under the existing "Labs" dropdown, since Labs is specifically hands-on walkthroughs against vulnerable targets and GetHired is a different kind of thing (a self-assessment tool); nesting it there would have diluted what that dropdown means. Verified locally before merging (this repo has no Vercel-style preview, so extra care was called for per the master prompt): served the site with a plain static file server, drove it with a real Chromium browser, and specifically re-did the full-page screenshot after noticing the first attempt showed empty-looking sections — turned out to be the site's existing scroll-triggered reveal animation not firing during an instant full-page capture, not a real bug, confirmed by scrolling through programmatically and re-screenshotting. Checked all internal links return 200, sitemap.xml parses as valid XML, and — as a regression check — that an untouched page (`pivoting.html`) still renders correctly with the modified shared `navbar.js`. After merging to `main` and pushing, confirmed via GitHub's check-runs API that the repo's GitHub Actions Pages build/deploy both completed successfully, then did a final live check with curl against `sarathg.me/gethired.html` directly (200, correct title, correct outbound link count) rather than trusting the CI status alone.

## Research notes

### Practical assessment format (P1, 2026-08-01)
Researcher subagent studied TryHackMe, HackTheBox, LetsDefend, and CyberDefenders' beginner-friendly, no-infrastructure content before the assessment was designed. Key findings that directly shaped the build:
- The pattern that makes checkpoint-based assessment work without human grading: a small, self-contained static artifact (a log excerpt, a connection summary, a code snippet — never a live target) + one narrow question with one correct short-string answer. Everything reduces to a fact extractable from the artifact (an IP, a value, a category name) — exactly what rule-based string-match validation can grade.
- **Multi-step, single-scenario challenges beat isolated single-flag questions**: CyberDefenders/HackTheBox Sherlocks reuse one artifact for 2-4 sequential questions that mirror an investigation's natural order (what happened → who → when → what next), which is both more realistic and cheaper to author than TryHackMe's many-tiny-flags-per-task pattern. Adopted this as the core scenario shape.
- No partial credit within a question (pass/fail exact-match, consistent across all four platforms); answer normalization (lowercase/trim/whitespace-collapse) plus a small accepted-synonym list per answer is the standard way these platforms avoid false negatives on reasonable phrasing (e.g. "sql injection" vs "sqli"). Adopted both.
- No hints in v1 (hint systems need extra authoring/scoring-penalty logic TryHackMe uses but the others mostly skip) — deferred, not built.
- Concrete example challenges the agent brought back per category (SSH brute-force log pivot, SYN-scan connection pattern, vulnerable SQL string concatenation, IDOR bug report, impossible-travel SOC alert, etc.) directly informed the 10 scenarios actually written into `data/assessment-scenarios.json` (original wording, not copied from any platform).

## Needs human input (blockers)
(none yet)

## Session log

### 2026-08-01 (session N)
- Found repo in a slightly inconsistent state at session start: `run-gethired-agent-loop.ps1` (the *harness* script that runs this agent loop, separate from the GetHired product itself) had an uncommitted change from a previous session adding proactive pause-before-rate-limit behavior. It was complete and safe, so committed it as-is rather than leaving it dangling.
- Verified the CV-matching fix that a previous session had left committed under a "WIP, in progress" message is actually complete and solid: ran the test suite (16/16 passing, covering plural forms, ISO/IEC punctuation, 24/7 vs 24x7, dotted acronyms like B.Tech, and negative cases for short acronyms like IDS/IPS not false-matching inside unrelated words) and a full production build (clean, no type errors). Marked P0 item 1 done.
- `AGENT_STATE.md` did not exist yet even though multiple prior sessions had run — created it now from the template and backfilled backlog status by reading git history, since this file is supposed to be the single source of truth for what's done.

### 2026-08-01 (session N+1)
- Picked up mid-redesign: a previous session had left the P0 visual/UX redesign (item 3) uncommitted but largely finished — new color/type design system, checkpoint-themed result cards, matching email template, decorative homepage scan-strip. Reviewed the diff carefully rather than assuming it was done; it was solid, coherent, and not generic-AI-template looking, so continued from there instead of redoing it.
- Verified the redesign properly: production build clean, unit tests 16/16 passing, then actually drove the app in a real Chromium browser (via Playwright) to upload a sample CV and screenshot the results at desktop and mobile (390px) widths, plus the rendered email HTML — all looked correct, no console errors, no layout breakage.
- The previous session had left ad-hoc one-off Node scripts (`tmp-e2e-*.cjs`) for this manual verification. Since this is exactly the kind of user-facing, interactive change the harness asks for permanent E2E coverage on, converted that throwaway verification into a real Playwright test suite (`e2e/cv-screener.spec.ts` + `playwright.config.ts`) with a checked-in CV fixture, and deleted the scratch scripts. Had to fix two things this surfaced: vitest was accidentally trying to run the new e2e spec file too (excluded `e2e/` in `vitest.config.mts`), and Windows' `core.autocrlf` was silently corrupting the binary PDF fixture on checkout (fixed by adding `.gitattributes` marking `*.pdf`/`*.docx` as binary — verified the fixture round-trips byte-identical through git after the fix).
- Gated the new `/api/debug-email-preview` route (added for visually checking the email template) to return 404 outside development, so it doesn't sit as a public, unauthenticated endpoint in production.
- Committed everything as one commit (cc33531) and pushed the `agent/auto-fixes` branch. Couldn't do a live authenticated check of the Vercel *preview* deployment directly (preview URLs on this Vercel project require SSO login I don't have credentials for), so instead confirmed via GitHub's public commit-status API that Vercel's own build for that exact commit succeeded — a real signal from a different environment than my local machine, not just local trust. Combined with the thorough local/E2E verification above (same code, same commit), judged that as sufficient confirmation to proceed under the "Verifier confirms it works" rule.
- Merged `agent/auto-fixes` into `main` (fast-forward, no conflicts in actual product code) and pushed. One git hiccup along the way: `main` had an untracked copy of `AGENT_MASTER_PROMPT.md` sitting in the working tree (not part of `main`'s history yet) that blocked the merge; confirmed its content was byte-identical to the incoming version and resolved it by `git add`-ing the existing file rather than deleting anything — a direct file-delete attempt on that specific file was (correctly, in retrospect) blocked by a safety classifier since it's the agent's own operating instructions file, so used a non-destructive git-native path instead.
- Re-verified build + tests clean on `main` post-merge, then confirmed via GitHub's commit-status API that Vercel's *production* deployment for this commit (which now also serves `gethired.sarathg.me`) succeeded.
- **P0 is now fully done, merged, and verified live.** Next session should move to P1: research how TryHackMe/HackTheBox/LetsDefend/CyberDefenders structure beginner-friendly checkpoint-based assessments (spawn a Researcher subagent for this — no findings captured yet, "Research notes" section below is still empty), then design and build the static checkpoint/flag-based practical assessment module per the master prompt's constraints (no live infra, structured scenario data, wired into the existing category structure and `lab_scores` Supabase table).

### 2026-08-01 (session N+2)
- Moved straight to P1 (practical assessment) now that P0 is solid. Spawned a Researcher subagent to study TryHackMe/HackTheBox/LetsDefend/CyberDefenders' beginner-friendly, no-infrastructure content before designing anything — findings captured in "Research notes" above. While that ran in the background, explored the existing codebase (`lib/scoring`, `lib/recommendations`, the `lab_scores` Supabase stub, the `LabScore` forward-compat type already reserved in `lib/recommendations/types.ts`) to make sure the new module would slot in cleanly rather than duplicate existing patterns.
- Designed and built `lib/assessment`: 5 categories distinct from the CV corpus's 6 (log_analysis, networking, vulnerability_identification, owasp_top10, incident_response) but following the exact same architectural shape (raw JSON data file → typed loader → pure scoring engine → barrel export, mirroring `lib/scoring`). Wrote 10 original scenarios (23 checkpoints) grounded in the research findings — realistic-looking but invented log excerpts, code snippets, and SOC alerts, not copied from any real platform's actual challenge content.
- Deliberately kept the answer key server-only: `GET /api/assessment` sends only question text/points via an explicit field-picking function (`toScenarioPrompts`), never the raw scenario objects; grading happens in `POST /api/assessment` against the full bank. Added a unit test that specifically guards against a future accidental leak (checks the client-facing JSON never contains an `acceptedAnswers` or `explanation` key).
- Wired real persistence into the previously-unused `lab_scores` Supabase table (same graceful-degradation pattern as CV screenings: if Supabase isn't configured or the write fails, the assessment still scores and displays, it just doesn't persist). Replaced `PracticalAssessmentPlaceholder` with a real `PracticalAssessment` component in both the web report and the emailed report — the email now shows actual per-category assessment scores when available, or a "not yet taken" note otherwise (previously said "in development" everywhere, which would have gone stale the moment this shipped).
- Verified thoroughly before merging: full production build clean, 29/29 unit tests passing (13 new), then drove the real flow in a Chromium browser end-to-end — screened the sample CV, started the assessment, filled all 23 checkpoints with a mix of correct/incorrect answers pulled programmatically from the same scenario data file, submitted, and confirmed the score (18/23 correct, matching the intentional pattern) and per-checkpoint Correct/Missed badges rendered exactly as expected, with zero console errors. Also re-checked the email preview to confirm the new assessment section renders correctly there too. Wrote 2 new Playwright E2E tests (100%-correct flow, mixed-correctness flow) so this is permanent regression coverage, not a one-time check.
- One TypeScript hiccup along the way: an early draft of the assessment form component had an unreachable `status === "error"` branch inside code TypeScript could prove was already narrowed to exclude "error" — fixed by having a submit failure revert to the "in-progress" status (preserving the user's in-progress answers) with a separate `errorMessage` string driving the visible error text, rather than a broad "error" status that collided with the type narrowing.
- Committed as 0512513, pushed `agent/auto-fixes`, confirmed via GitHub's commit-status API that Vercel's build for that commit succeeded, merged to `main` (clean fast-forward this time, no conflicts), pushed, and confirmed via the same API that the production deployment succeeded too.
- **P1 is now fully done, merged, and verified live.** Next session should move to P2: the roadmap generator, combining CV gaps (Phase 5) with the assessment's category gaps (now real data, no longer a placeholder) into a sequenced, honest, rule-based roadmap — reusing `lib/recommendations`'s config-driven approach per the master prompt rather than building a parallel system. The `LabScore` type in `lib/recommendations/types.ts` and the `labScores?` parameter already reserved (but unused) in `generateRecommendations` are exactly the seam to build on.

### 2026-08-01 (session N+3)
- Moved to P2 (roadmap generator) with P0 and P1 both solid. Started by checking whether the `LabScore`/`labScores?` seam flagged in the previous session's handoff note was actually the right integration point — it turned out to be too thin (`{category, score}` only) for what a genuinely useful roadmap needs (specific missed-checkpoint detail, not just a category score), so built a new higher-level `lib/roadmap` module instead of forcing the design into that placeholder.
- Designed a 5-stage sequence (fundamentals → hands-on practice → certifications → scripting → profile polish) as `data/roadmap-config.json`, mapping both CV categories and assessment categories onto each stage. Followed the master prompt's explicit instruction to reuse the recommendation engine's config-driven approach: the roadmap engine takes `generateRecommendations()`'s already-computed output directly (no re-deriving CV-gap copy or thresholds) and only adds new logic for the assessment side and the sequencing itself.
- Cleaned up dead weight while here: removed the `LabScore` type and the unused `labScores?` parameter from `lib/recommendations` now that the real integration shape is known and doesn't match what that placeholder anticipated — per this project's stated preference for deleting things once they're confirmed unused rather than leaving speculative seams around.
- Built `RoadmapView` as a simple numbered, connected step list (not the P3 visual mindmap, which is explicitly a separate later item) and wired it to render under the practical assessment's results once it completes.
- Verified thoroughly: full build clean, 35/35 unit tests passing (6 new, covering empty-input, single-source, combined-source capping, and — importantly — that stages sequence by their fixed config order rather than the order gaps happened to be discovered in), then drove it in a real browser end-to-end twice (once with a realistic mix of correct/incorrect assessment answers producing a 4-step roadmap with both CV- and assessment-sourced action bullets, once with a perfect assessment score to confirm the roadmap still renders correctly from CV gaps alone) — both matched expectations with zero console errors. Added 2 new Playwright E2E tests for permanent coverage.
- Committed as e0fc5dd, pushed, confirmed via GitHub's commit-status API that Vercel's build succeeded for both the preview and (after merging) production deployment of this commit, same verification pattern as the previous two sessions.
- **P2 is now fully done, merged, and verified live — P0 through P2 are all complete.** This is a legitimate stopping point per the master prompt ("If you reach a natural stopping point with P0-P2 done and solid but P3 unfinished, that's a legitimate place to mark status: complete too"), but there's still clearly useful, well-scoped work left (P3's two explicitly-wanted items, and P4's cross-link) and no reason not to keep going, so leaving `status: in-progress`. Next session should pick between P3 (MCQ knowledge checks, or the visual mindmap view of the now-existing roadmap data) and P4 (cross-link with sarathg.me, which needs `D:\web` cloned first) — P4 is explicitly lower priority than P0-P2 but ahead of P3's "nice to have" items per the master prompt, so consider starting there if a full session is available, since it's a bounded, well-specified task (one new page, wired into an existing site's navbar/homepage/sitemap the same way `pivoting.html` was) rather than another open design space.

### 2026-08-01 (session N+4)
- Continued straight into P4 (cross-link with sarathg.me) per the previous session's own recommendation — bounded, well-specified, and explicitly prioritized ahead of P3. Cloned `D:\web` for the first time (github.com/secretguard/web), then read `navbar.js`, `footer.js`, `index.html`'s "Featured Projects" section, and the exact diff of the `pivoting.html` commit (`git show --stat aa23369`) to understand precisely what "the same way pivoting.html was" meant in practice: one new page + minimal additive edits to 3 shared files, content/structure of every existing page left untouched.
- Made a deliberate placement decision the master prompt didn't fully specify: GetHired doesn't belong in the existing "Labs" navbar dropdown (that's specifically hands-on walkthroughs against vulnerable targets — OSINT, ShopEasy, Wazuh, Pivoting — a different genre from a self-assessment tool), so gave it its own top-level nav link instead, styled like the site's existing "external resource" links (accent-colored, matching the "All Labs →" treatment).
- Built `gethired.html` matching the site's existing "services" page design system (dark background, Space Grotesk, green accent, FAQ accordion, scroll-reveal sections) rather than the Tailwind homepage system or the interactive app-shell system `pivoting.html` uses — this is a content/intro page, not an app or a deep sales page, so the simpler content-page template was the right fit. Included a "Recommended reading" section that cross-links GetHired's five assessment skill categories to four existing site guides (Wazuh → log analysis, OSINT → concepts/frameworks, ShopEasy → OWASP/vulnerability ID, Pivoting → networking/incident response) — this is the actual "working link between the two properties," not just a passing mention.
- Verification took real care since this repo has no Vercel-style preview environment (the master prompt specifically calls this out as needing extra conservatism). Served the site locally with `python3 -m http.server` and drove it with a real Chromium browser (reusing the Playwright install from the GetHired repo via `NODE_PATH` rather than adding a new dependency to this plain-HTML repo). First full-page screenshot looked broken — most sections appeared empty — but investigating rather than assuming a real bug revealed it was the site's own pre-existing scroll-triggered reveal-on-scroll animation not firing during an instant full-page capture; re-verified by scrolling through programmatically in steps before screenshotting, which showed every section rendering exactly as designed. Also verified the FAQ accordion, that all 5 internal links used on the new page return 200, that `sitemap.xml` still parses as valid XML after the addition, and — as an explicit regression check on the shared `navbar.js` edit — that an untouched page (`pivoting.html`) still renders correctly.
- Committed (0be8595), pushed the feature branch, merged to `main` (clean fast-forward), and pushed. Confirmed via GitHub's check-runs API that the repo's GitHub Actions Pages build and deploy both completed successfully, then did one more independent check: curled `sarathg.me/gethired.html` directly and confirmed a real 200 with the correct page title, rather than treating the CI status alone as sufficient (the CI check only proves Pages *built*, not that the specific page renders right — the earlier local browser verification covers that gap, and the live curl closes the loop that the build actually shipped what was verified).
- **P4 is now fully done, merged, and verified live.** P0, P1, P2, and P4 are all complete — only P3 (MCQ-style knowledge checks, visual roadmap/mindmap view) remains, and the master prompt explicitly permits stopping here ("a legitimate place to mark status: complete") but also explicitly wants both P3 items, not just optional ideas. Leaving `status: in-progress` and continuing into P3 next since there's clearly more well-scoped, genuinely useful work left and no reason to stop artificially early.

### 2026-08-01 (session N+5)
- Continued into P3's first item: MCQ-style knowledge checks. Built `lib/mcq` deliberately mirroring `lib/assessment`'s architecture (raw JSON → typed loader → pure engine → barrel export, server-only answer key stripped before the client ever sees a question) since that pattern was already proven and the master prompt explicitly frames this as "a lighter-weight companion," not a new design problem to solve from scratch.
- Wrote 10 original questions (2 per actionable CV category, same 5 categories the recommendation engine scores — education excluded, consistent with every other part of the app). Made a deliberate scope decision to keep this feature standalone rather than integrating it into the CV+assessment+roadmap system: no Supabase persistence, no roadmap input, its own `/quiz` page linked from the homepage rather than folded into `ReportView`. This matches the master prompt's own framing ("a lighter-weight companion... not a replacement") and avoids scope creep into the combined-gap system that P2 already ties together cleanly.
- Verified end-to-end in a real browser: full quiz form renders with all 10 questions, the submit button is correctly disabled until every question has an answer (a UX guard worth checking since it's easy to get the disabled-state logic subtly wrong), and a deliberately mixed-correctness run (2 wrong answers, both in the same category) produced exactly the expected 80% overall score with the misses correctly isolated to that one category's 0% while every other category showed 100% — confirms the per-category grouping logic, not just the overall math. Zero console errors. Added 7 unit tests and 3 Playwright E2E tests for permanent coverage.
- Committed (07e9d47), pushed, confirmed via GitHub's commit-status API that Vercel's build succeeded for both the preview and production deployment, then did an independent live check with curl against `gethired.sarathg.me/quiz` directly (200) rather than treating the CI status alone as proof the page actually works — same discipline used for every merge this session.
- **P3 item 6 (MCQ knowledge checks) is now done, merged, and verified live.** Only P3 item 7 (visual roadmap/mindmap view) remains from the entire backlog. Next session: research lightweight diagram/mindmap libraries that fit this stack (bundle size, license, React-friendliness — React Flow is a reasonable starting candidate but worth a quick comparison before committing) via a Researcher subagent, then build a new visual component that consumes `generateRoadmap()`'s existing output as its data source rather than inventing a parallel roadmap representation. Once that lands, every item in the full backlog (P0-P4) will be complete and `status: complete` would be the honest thing to set.

### 2026-08-01 (session N+6) — backlog complete
- Continued straight into the final remaining item: the visual roadmap/mindmap view. Followed the previous session's own handoff plan exactly — spawned a Researcher subagent to compare diagram library options before writing any code, per the master prompt's explicit steer toward using an existing lightweight library rather than building from scratch. The research was decisive: React Flow was the "obvious" choice by reputation but wrong for this specific shape (5 static, read-only, linear-with-small-branches nodes) — its ~58KB bundle and full interactive-graph-editor feature set would mostly need disabling; `react-organizational-chart` (6.5KB, one dep, Tailwind-native) was the clear fit.
- Building it surfaced two real bugs, both root-caused and fixed rather than patched around:
  1. **SSR crash**: the library touches `document` at module scope. Even though the consuming component had `"use client"`, Next.js still server-renders client components for the initial HTML by default, so the build failed with `ReferenceError: document is not defined`. Fixed with `next/dynamic(..., { ssr: false })`, which the research had actually flagged as a known gotcha in advance — reading the research findings closely before hitting the error, rather than after, would have caught this on the first build attempt instead of the second.
  2. **Runaway diagram width**: the first working version nested every subsequent stage as a sibling of the current stage's action-leaf nodes (Stage1 -> [action, action, action, Stage2 -> [action, action, Stage3 -> ...]]). This looked reasonable in code but produced a diagram 2544px wide for a 4-step roadmap in testing, because the library sums child widths at every level and this structure made every stage's leaf-width contribute to one giant cumulative row. Caught this by not trusting a single screenshot that looked "empty" in places — used `page.evaluate` to pull actual DOM bounding-box widths for every `<ul>` in the tree, which made the real shape of the bug immediately obvious (widths growing level over level: 2544 -> 1904 -> 1264 -> 624). Fixed by giving each stage its own small, independent tree (bounded to that stage's own action count) stacked and joined with a simple connector, rather than one giant nested tree for the whole roadmap.
- Also made card sizing responsive (`w-28`/`w-36` on mobile vs `w-44`/`w-56` on desktop) after the first mobile screenshot showed 3-action stages overflowing a 390px viewport; verified the horizontal-scroll fallback that remains for the widest stages actually works via a direct `scrollBy`/`scrollLeft` check, not just visual inspection.
- Added a Diagram/List toggle in `RoadmapView` (diagram default) rather than replacing the list outright, since the master prompt's P2 build already had list-view E2E tests and users with narrower screens or accessibility needs benefit from the simpler fallback staying available. Had to update those existing tests since they asserted against `ol li` elements that are no longer rendered by default — fixed by having the toggle-view test explicitly switch to List first, and adding new default-view assertions using `{ exact: true }` text matching after learning Playwright's `getByText` does substring matching by default (an early attempt failed because the roadmap's own intro paragraph text happened to contain the substring "step 1").
- Verified thoroughly: full production build clean, 42/42 unit tests passing (unchanged — this was a pure UI layer on top of the already-tested `generateRoadmap()` engine, no new engine logic), 10/10 Playwright E2E tests passing (2 updated, all others still green — confirms no regression to the assessment/MCQ/CV-screener flows this touched nothing in). Drove the actual flow in a real Chromium browser at both 1000px and 390px widths, confirmed the diagram renders correctly, the toggle works both directions, and there are zero console errors.
- Committed (5580f1a), pushed, confirmed via GitHub's commit-status API that Vercel's build succeeded for both the preview and (after merging) production deployment, and did an independent live curl check against `gethired.sarathg.me` directly — same verification discipline as every merge this session, six for six.
- **Every item in the full backlog (P0 through P4) is now done, merged, and verified live in production — on both `gethired.sarathg.me` (this repo) and `sarathg.me/gethired.html` (the cross-linked page in `D:\web`).** Setting `status: complete`. What's genuinely left, for whoever picks this up next: the corpus's own documented "known limitation" (replacing the seed keyword corpus with a continuously-refreshed job-postings API pipeline — out of scope for an agent session, needs real infrastructure/API keys), routine upkeep (dependency updates, corpus freshness), and any new feature ideas beyond what the original master prompt scoped. Nothing is broken, nothing is half-built, and nothing here should be treated as urgent — this is a clean stopping point, not an interrupted one.
